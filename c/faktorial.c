#include <stdio.h>
#include <limits.h>

int faktorial(n) {
    return 0;
}

int main() {
    char str[100];
    printf("Zadajte cislo");
    fgets(str, 4, stdin);
    printf("%s", str);
    printf("%d", LONG_MAX);
}
