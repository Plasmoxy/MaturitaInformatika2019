#include <stdio.h>
#include <stdlib.h>
#include <limits.h>

const int NSTR = 100;

int faktorial(int n) {
    return 0;
}

int main() {
    printf("Zadajte cislo: ");
    char str[NSTR];
    fgets(str, NSTR, stdin);


    printf("%d\n", atoi(str));
}
